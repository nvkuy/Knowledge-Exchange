import 'package:flutter/material.dart';

class AddKE extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: new Center(
        child: Text('add KE'),
      ),
    );
  }
}